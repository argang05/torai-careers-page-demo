import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Salesforce Careers India | Join Our Team",
  description: "Explore exciting career opportunities at Salesforce India. Join our team and be part of a company that values innovation, equality, and customer success.",
  keywords: "Salesforce careers, jobs in India, tech jobs, Salesforce India, career opportunities",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
